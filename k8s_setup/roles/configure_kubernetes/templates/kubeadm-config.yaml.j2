apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
kubernetesVersion: "stable"
controlPlaneEndpoint: "{{ hostvars[groups['control1'][0]].ansible_default_ipv4.address }}:6443"
networking:
  podSubnet: "{{ pod_network_cidr }}"
